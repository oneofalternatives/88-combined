# validator — notes

Not written. Idea: the book over-determines itself, so check it against itself.

- times increase down each direction column (mind midnight)
- отпр >= приб at a stop
- implied speed between stops, Δkm/Δt — only the 12 `distance` halves carry km
- same leg across many trains: times should cluster, flag outliers
- distance -> station name should agree book-wide

Output is triage — ranked "look here", not a verdict. The data is unchecked OCR.
